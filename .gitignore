# Secrets and personal data
.env
.env.*
!.env.example
# Vonage application private keys
*.key
data/neighbors.local.json
# Live mode's last Deepfire spread answer (backend/app/live_spread.py), rewritten every few minutes
data/live_spread.json
data/live_spread.tmp
# Live mode's places at risk per Deepfire run (backend/app/live_operations.py)
data/live_places/
# The audit log of every decision and outcome (backend/app/audit.py, docs/setup/operations.md)
data/audit.jsonl
NOTES.internal.md

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node
node_modules/
dist/
# Rendered videos (video/README.md)
video/out/
*.tsbuildinfo

# Claude Code worktrees for background agents
.claude/worktrees/

# Editors and OS
.vscode/
.idea/
.DS_Store
# Live mode's last DGT answer: forest-fire incidents and closures (backend/app/live_dgt.py)
data/live_dgt.json
data/live_dgt.tmp

# Screenshots dropped here while testing; never part of the build.
errors/
